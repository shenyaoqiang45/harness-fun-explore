---
description: 修改 UI/服务端后打开网页做端到端验证
alwaysApply: true
---

# 修改后打开网页测试

在 Cursor 中完成 UI、客户端或服务端相关修改后，除跑 `npm test` 外，必须用浏览器打开应用做端到端验证。

## 本项目

- 启动：`npm run dev`（默认 **http://localhost:3000**，可用 `PORT` 覆盖）
- 若服务已在跑但代码刚改过：先 `npm run build` 再重启 dev，避免旧 bundle

## 验证流程

1. 确认 dev 服务已启动（终端出现 `Server running at http://localhost:3000`）
2. 用浏览器 MCP 打开 `http://localhost:3000`
3. 至少验证：
   - 页面加载无 `/client.js` 等静态资源错误
   - 输入种子词并点击 **Launch**，树/波浪布局正常渲染
   - 叶节点完整可见（不被裁剪）
4. 若失败：查终端与浏览器控制台，修复后重跑测试并重复浏览器验证

## 原则

- 不要仅凭单元测试就认为 UI 可用
- 测试失败或页面异常时继续调查修复，直到测试与网页验证均通过
